using UnityEngine;
using UnityEngine.SceneManagement;
using System.Collections;

public class LoadingScreen01Script : MonoBehaviour {

	// Use this for initialization
	void Start () {
        SceneManager.LoadScene(2);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
